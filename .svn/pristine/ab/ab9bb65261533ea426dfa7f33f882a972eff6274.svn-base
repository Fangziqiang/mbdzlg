function show(c_Str,imgg){
	if(document.all(c_Str).style.display=='none'){
		document.all(c_Str).style.display='';
        document.all(imgg).src='../images/up.jpg';
    }else{
		document.all(c_Str).style.display='none';
        document.all(imgg).src='../images/down.jpg';
	}
}